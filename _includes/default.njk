---
title: Cooking with Sandor
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style.css">
    <title>{{ title }} </title>
  </head>
  <body>
  {{ content | safe }}
  {% if tags %}
  <h4>Tags</h4>
    {% for tag in tags %}
    <a href="/tags/{{tag}}">{{tag}}</a>
    {% endfor %}
  {% endif %}
  </body>
</html>
